/* Week 1 intentionally keeps JavaScript minimal.
   Future interactive features should be added as small modules instead of one
   large script. Bootstrap handles the responsive navbar in this version. */

document.addEventListener("DOMContentLoaded", () => {
    document.documentElement.classList.add("js-ready");
});
