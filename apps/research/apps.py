from django.apps import AppConfig


class ResearchConfig(AppConfig):
    """Research experiment UI: attacks, detection, defence, and evaluation."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.research"
